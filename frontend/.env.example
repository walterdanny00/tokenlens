# Optional. Where the TokenLens backend lives. Defaults to the Render deployment.
VITE_API_URL=https://tokenlens-sxdq.onrender.com

# Optional. The Telegram bot the "Watch on Telegram" button opens (without the @).
VITE_TELEGRAM_BOT=tokenlens_bot
